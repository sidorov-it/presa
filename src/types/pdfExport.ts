export type PdfExportStrategy = 'per-slide' | 'single-page-test';

export const DEFAULT_PDF_EXPORT_STRATEGY: PdfExportStrategy = 'per-slide';

export const SINGLE_PAGE_TEST_STRATEGY: PdfExportStrategy = 'single-page-test';
